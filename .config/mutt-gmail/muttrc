set imap_user = "jace42@gmail.com"
set smtp_url = "smtp://jace42@smtp.gmail.com:587"
set from = "jace42@gmail.com"
set realname = "Jacob Certain"

set folder="imaps://imap.gmail.com:993"
set spoolfile="+INBOX"
set record="+[Gmail]/Sent Mail"
set postponed="+[Gmail]/Drafts"
set ssl_force_tls=yes # Always use SSL when connecting to a server

set header_cache=~/.config/mutt-gmail/hcache # Cache mail headers, faster loading
#set header_cache_pagesize=64000 # Supposed optimum is 32k
set certificate_file=~/.config/mutt-gmail/certificates

set move=no # Stop "move read messages to mbox"

set imap_keepalive=200

auto_view text/html

source ~/.config/mutt-gmail/password
source ~/.config/mutt-gmail/aliases
source ~/.config/mutt-gmail/bindings
source ~/.config/mutt-gmail/colours
source ~/.config/mutt-gmail/common
#source ~/.config/mutt-gmail/hooks
source ~/.config/mutt-gmail/macros
